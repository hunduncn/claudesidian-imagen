// src/routes/vault.ts — STUB: Task 10 replaces this with real implementation
import type { ServerContext } from "../server.ts";

export async function handleVaultRoutes(_req: Request, _ctx: ServerContext): Promise<Response> {
  return new Response("Not Implemented (vault routes)", { status: 501 });
}
